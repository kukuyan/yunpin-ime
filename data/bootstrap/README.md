# Bootstrap vocabulary

`public_seed.tsv` is a tiny project-authored set of ordinary phrases used for smoke tests and first-run fallback. It contains no exported personal data and is released under Apache-2.0 with the rest of YunPin's original data tooling. Full public dictionaries are built offline from the locked sources documented in `docs/DATA_SOURCES.md`.
